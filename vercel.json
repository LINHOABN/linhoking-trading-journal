{
    "version": 2,
    "buildCommand": "cd frontend && npm install && npm run build && mkdir -p ../backend/app/static && cp -r dist/* ../backend/app/static/",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/main.py"
        }
    ]
}