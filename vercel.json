{
    "version": 2,
    "buildCommand": "cd frontend && npm install && npm run build && cd .. && python bundle_assets.py",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/main.py"
        }
    ]
}